# Введите ваше 8-разрядное число здесь
n = 12345678
# Преобразуем число в строку и используем функцию map для преобразования каждой цифры в целое число
digits = map(int, str(n))
# Суммируем все цифры с помощью функции sum
total = sum(digits)
# Выводим результат
print("Сумма цифр числа:", total)
